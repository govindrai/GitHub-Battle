import React from "react";

export default function Results(props) {
  return (
    <div>
      {console.log(props)}
      {/*      {match.search}
      <img
        className="profile-image"
        src={`https://github.com/${match.search.username}.png?size=200`}
      />
      <div className="username">{match.search.username}</div>*/}
    </div>
  );
}
